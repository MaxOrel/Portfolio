.skills-title-container {
    &.blocked {
      opacity: 0.5;
      filter: grayscale(100%);
      pointer-events: none;
    }
  }
  .skills-card-title {
    display: flex;
    justify-content: space-between;
  }