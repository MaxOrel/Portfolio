.hero {
  padding-top: 54px;
  position: relative;
}

.hero__container {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.hero__content {
  flex: 0.95;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 24%;
  @include desktop {
    flex: 0.85;
  }
  @include tablets {
    flex: 0.9;
  }
}

.hero__downbutton {
  margin-top: auto;
  margin-bottom: 6%;
  display: flex;
  justify-content: center;
  @include desktop {
    margin-top:inherit;
    margin-bottom: 8%;
  }
  @include tablets {
    display: none;
  }
}